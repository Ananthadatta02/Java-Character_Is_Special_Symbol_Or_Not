
# Java-Character_Is_Special_Symbol_Or_Not
This Java program checks whether an entered character is a special symbol or not. It takes a single character input from the user and verifies if it is not an alphabet (A-Z, a-z) or a digit (0-9). If it's a special symbol, it prints the character and its ASCII value; otherwise, it prints a message.
